This is a testing branch with a minimal
ReGeX server. It has no external dependencies,
an is setup and run as follows:

  $ go build # and wait
  $ ./redb < /path/to/word/list
  Read 99172 words. Starting server... 
  Suggested "w.*"
  Suggested "(a|.*)l[^xy]"
  ...

For licensing, refer to LICENSE

---
phikal, 2016
